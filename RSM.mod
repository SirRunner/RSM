name = "Random **** Mod" # name of your mod, shown in launcher
path = "mod/RSM" # location of your mod files 
user_dir = "RSM" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up  
